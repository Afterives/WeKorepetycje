<!DOCTYPE html>
<html lang="pl">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>WeKorepetycje - dashboard</title>
    <meta
      name="description"
      content="Strona do rezerwacja korepetycji, zapraszam!"
    />
    <meta
      name="keywords"
      content="Nauka, korepetycje, edukacja, wykształcenie, matura, podstawa, rozszerzenie"
    />
    <meta name="author" content="Przemysław Róg" />

    <link rel="stylesheet" href="css/style_dashboard.css" />
    
  </head>

  <body>
    <h1>Koszyk użytkownika <%= user %></h1>
    <ul>
      <% show_cartsk.forEach(function(element) { %>
        <li><%= element.courseid %> - <%= element.teacherid %> - <%= element.wartosc %></li>
      <% }); %>
    </ul>
    

    </body>
    </html>